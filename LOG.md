# LOG — chronological journal

Newest entries at the bottom. Every attempt gets an entry, including failures.

---

## 2026-07-21 — Session 0: setup and triage

**Environment provisioned**
- Python 3.12.13 venv at `.venv` with `sympy`, `numpy`, `networkx`, `z3-solver`,
  `python-sat`, `pyyaml`.
- `nauty` 2.9.3 via homebrew (`geng`, `gentreeg`) for isomorph-free graph generation.
- `elan` → Lean 4.32.0 + mathlib, project at `ErdosFormal/`.
- Machine: 10 cores, 16 GB.

**Problem source.** Rather than scraping 1217 pages I cloned the community mirror
<https://github.com/teorth/erdosproblems>, whose `data/problems.yaml` is the
declared ground truth for problem metadata. Statements are fetched from
erdosproblems.com and cached in `data/statements/`.

**Triage.** `tools/triage.py` buckets all 1217 problems by status. 43 are flagged
settleable by finite computation (`decidable` 9, `falsifiable` 27, `verifiable` 7).
I read all 43 statements and classified each by applicable approach with runtime /
P(machine-verifiable) / novelty / scaling estimates — all recorded in
`RESEARCH_TRACKER.json` (generated by `tools/seed_tracker.py`, which keeps the
reasoning next to the number it produces).

Deliberately rejected as NOT COMPUTE-AMENABLE, with reasons: [107] happy-ending
(f(7) needs 33-point configurations), [242] Erdős–Straus (already 10¹⁸),
[364] three consecutive powerful numbers (already 7.4×10²⁸), [723] projective
plane of order 12 (known multi-CPU-year), [19] Erdős–Faber–Lovász (the "finite
check" is finite only in principle), [114]/[1041]/[106] (continuous, no exact
certificate). [779] was rejected on *verifiability* rather than cost: extending it
means primality of thousands-of-digit numbers, and a BPSW pseudoprime is not a
proof, so the artifact would fail the trust rule.

---

## [647] Erdős–Selfridge — VERIFIED (exhaustive negative)

**Key step.** `max_{m<n}(m + τ(m)) ≤ n+2` rewrites as `τ(n−j) ≤ j+2 for all j ≥ 1`.
Since `τ(m) ≤ 6720` for `m < 10¹²` (A066150), only `j ≤ W = 16384` can matter.
That makes the predicate *local*, so the range splits across cores with a
`W+2` overlap. The C program measures the largest τ it actually meets and
`progress647.py` refuses to report a bound if `max τ + 2 ≥ W` — the cutoff is
checked, not assumed.

**Cross-check.** `verify647.py` is an independent brute force (sieve τ by
counting multiples, track the running maximum, no locality argument). On
`[2, 10⁶]` it and the C search agree exactly: `{2,3,4,5,6,8,10,12,24}`.

**Throughput.** 1.75 s per 10⁸ on an idle machine at 10¹³; ~7 s per 10⁸ once ten
CPU-bound processes are competing. Observed ≈1.1×10¹¹/hour with 8 workers.
I originally aimed at 10¹³; at the real rate that is ~90 hours, so the honest
target is ~10¹². (Early on I misread `ls` output sorted alphabetically as
chronological and briefly thought the job had stalled — it had not.)

**Not attempted:** a staged rewrite (prime-sieve for `n−1`, then Sophie-Germain
filter for `n−2`, then factor the rest) would likely be 10–100× faster, since
`n` works only if `n−1` and `(n−2)/2` are both prime. Noted for later.

---

## [287] Unit-fraction gaps — VERIFIED (Lean reduction + finite check)

**Dead end first.** Direct DFS over gap-≤2 chains with exact rational sums is
exponential; it stalls around max(S) ≈ 40. Recorded as a dead method, kept only
to settle max(S) ≤ 37.

**What worked.** A p-adic valuation argument: if `Σ 1/n = 1` and `T` is the set
of members with maximal `v_p`, then `Σ_{n∈T}(n/p^e)⁻¹ ≡ 0 (mod p)`. For
`p > √b` this forces `p | Σ_{j∈J} L/j`, so for `p > Bound(m)` no multiple of `p`
is in `S` at all. Two *consecutive* excluded integers then contradict "gaps ≤ 2".

**Then the argument was rewritten to be Lean-friendly.** Instead of the lcm of
all of `S`, use the explicit common multiple `D = 2pL` where `L` is the lcm of
the members not divisible by `p`. Every term `D/n` is then visibly a multiple of
`p` except the two special ones, and the whole thing collapses to `p | cL` with
`1 ≤ c ≤ 3 < p`. No valuation machinery at all.

**Formalized** in `ErdosFormal/ErdosFormal/Erdos287.lean` — `elimination`,
`certificate_sub`, `certificate_add`, all with no `sorry` and only
`[propext, Classical.choice, Quot.sound]`.

**Both witness forms turned out to be necessary.** I first formalized only the
`2p−1` case, then checked whether it alone covers every `b`: it does not — there
is a hole at `b = 57`. So `certificate_add` was added to match what the C
covering actually does.

**Off-by-one caught.** `cover287.c` takes `b0` = *first* `b` needing coverage.
Calling it with `37` reports a spurious hole (the first interval starts at 38);
the correct invocation is `./cover287 38 B`. Documented in the source.

Bound reached: max(S) ≤ 10¹⁰ confirmed; a 10¹² covering is running.

---

## [458] lcm inequality — VERIFIED to 10¹⁴

**Reduction.** `lcm(1..p_{k+1}−1)/lcm(1..p_k)` is *exactly* the product of the
primes `q` having a power `q^e` (`e ≥ 2`) inside the gap `(p_k, p_{k+1})` — there
are no primes in that interval, so every prime power there has `e ≥ 2` and
contributes one extra factor of `q`. The conjecture is literally `∏ q < p_k`.

**Why it is cheap.** Only ~`π(√X)` prime powers exist below `X` (670,121 below
10¹⁴), so walk the prime powers rather than the ~10¹² prime gaps. Grouping two
prime powers into the same gap is decided by *exhibiting* a prime between them,
never by assuming one exists.

Only 5 gaps below 10¹⁴ contain two prime powers — `{8,9}`, `{25,27}`,
`{121,125}`, `{2187,2197}`, `{32761,32768}` — and the tightest case overall is
the very first, `p_k = 7`, ratio 6/7. Runtime 3 minutes.

---

## [617] Erdős–Gyárfás balanced colourings — DEAD END for the open case

Encoding: `x[e,c]`, exactly-one colour per edge, plus "every `(r+1)`-subset sees
every colour" (equivalently: `K_n` minus each colour class is `K_{r+1}`-free).
Validated against the known `r = 3` theorem: UNSAT.

**Symmetry breaking mattered enormously.** The group is `S_n × S_r` (26!·120 at
r = 5). Sorting the star at vertex 0 — legitimate, since relabelling vertices
1..n−1 is a symmetry — cut r = 3 from **47 s to 0.3 s**, a 150× speedup. A
redundant Turán cardinality constraint (`|colour class| ≥ 55` at r=5) made no
measurable difference.

r = 4, also a known case, had still not finished after ~1.75 hours, at which
point I killed it to free a core (so it is a lower bound on the runtime, not a
measured one). r = 5 (K₂₆, 5
colours, 1625 vars / 1.15M clauses) is therefore far out of reach for UNSAT, and
r = 5 is the first *open* case. Recorded as a dead end. A SAT answer would still
be trivially checkable — `check_colouring()` re-verifies a claimed colouring from
scratch — so a local-search attempt remains open as future work.

---

## [993] Tree independence sequences — REJECTED at triage

Literature has verified unimodality to 29 vertices (2026). n = 30 is 1.48×10¹⁰
free trees × O(n²) DP ≈ 1.3×10¹³ operations for a one-vertex improvement. Not run.

---

## [699] Erdős–Szekeres gcd of binomials — scanning

`v_p(C(n,i))` is read off as the number of carries adding `i` and `n−i` in base
`p` (Kummer), so no binomial coefficient is ever formed. For each `n` the sets
`A_p = {i : p | C(n,i)}` are bitmasks and the covered-`j` set for a given `i` is
one OR over the relevant primes.

Self-test reproduces two documented facts: `gcd(C(28,5),C(28,14)) = 2³·3³·5`
(with `p = 5` satisfying `p ≥ i` and *no* prime satisfying `p > i`, exactly as the
problem text says), and Sylvester–Schur for all `n < 120`. No counterexample for
`n ≤ 800`; a scan to 3000 is running.

---

## [488] Density of multiples — scanning

Exact integer test (`n·f(m) < 2·m·f(n)`, no floats). Enumeration restricted to
divisibility *antichains*, since `a | a'` makes `a'` contribute no new multiples.
Reproduces the known extremal configuration immediately: `A = {a}`, `n = 2a−1`
gives ratio `2 − 1/a` (observed 39/20 at `a = 20`). Larger scan running.


---

## 2026-07-22 — Session 1: sync phase, ROI triage, knowledge graph

**Three jobs survived the session break and completed.**
- [366] finished the full 10²⁴ run: 460,160,082 cubefull numbers examined,
  exactly two hits, both already known (8|9, 12167|12168) and **both in the
  opposite order** to the one the problem asks about. Published bound was 10²²,
  so this is a 100× extension.
- [287] covering completed at 10¹² (1,321,198,806 witness primes, no hole).
- [647] kept running to 1.44×10¹² and is still extending.

**SYNC PHASE built** (`tools/sync.py`). Diffs upstream `teorth/erdosproblems`
against the local snapshot (added / removed / status-changed / statement drift)
and reads the official AI-contributions wiki page. First run: no upstream
changes; 11 of my problems appear on the AI page. Two mattered:
- **[835]** has an AlphaProof contribution logged as *"likely folklore"* — so my
  Steiner-large-set reduction may well BE that folklore. Recorded, so novelty is
  not overclaimed.
- **[366]** has an automated-pipeline entry (Apr 2026) that is a *conditional*
  partial result, not a search bound — my exhaustive bound is a different artifact.

A wrinkle worth noting: the wiki page title contains "Erdős", and urllib raises
on the non-ASCII character before it ever reaches the network. The first run
reported the page as unavailable rather than silently treating it as empty —
which is the behaviour I want when a novelty check fails.

**ROI TRIAGE** (`tools/upgrade_tracker.py`). Priorities are now
`expected_value × novelty × p_verifiable / compute_cost`, with compute_cost on a
log scale (1 minutes, 3 hours, 10 days, 30 infeasible). Added
`published_search_bounds`, `prior_work`, `verification_artifacts`,
`reusable_modules_created`, `knowledge_dependencies`, `related_problems`, and a
14-edge **knowledge graph** linking problems through shared modules and tricks.

**Two problems closed on NOVELTY without spending compute** — which is the point
of doing the sync first:

- **[398] Brocard–Ramanujan.** The erdosproblems page says "no other solutions
  below 10⁹". The literature is at **10¹⁵** (Epstein & Glickman, after Matson's
  10¹²). Merely matching that is ~2×10¹⁵ modular operations. DEAD-END, recorded
  with the page/literature discrepancy. Had I trusted the page I would have
  burned hours to land six orders of magnitude short of the record.

- **[475] Graham's rearrangement** — nearly the same story, but it survived. The
  page says "proved for t ≤ 12"; the literature is at **|A| ≤ 20 in any abelian
  group** (Costa–Pellegrini). Combined with the settled range p−3 ≤ t ≤ p−1, the
  open window is only `t ∈ [21, p−4]`, which is **empty for p ≤ 23**. So the
  first genuinely open primes are p = 29 (1,682,811 subsets) and p = 31
  (22,963,621); p = 37 needs 1.4×10¹⁰ and is out of reach.

**[475] search design — the interesting part.** A naive ascending DFS is
exponential in exactly the open regime: p=17, t=12 costs ~175 ms per subset,
while t=8 costs 0.18 ms. Randomised greedy with restarts costs 0.04 ms — a
**4000× speedup** — and empirically never fails. But a heuristic failing is not
evidence that no ordering exists, so `find_ordering` falls through to a
**complete DFS**, and only DFS exhaustion may report "none". Every ordering found
is re-checked by a separate function that recomputes the partial sums.

That rule was extracted into **`lib/erdos/search.py`** (`greedy_then_complete`):
*a heuristic may only ever answer YES; only exhaustive search may answer NO*.
Cross-checked against the problem-specific code on 70,653 instances — zero
disagreements. It applies directly to [743] tree packing and [835] Johnson
colouring, and those edges are in the knowledge graph.

**Operational incident, recorded because it nearly cost a result.** To free the
machine I stopped the [647] driver, intending to let the eight in-flight chunks
finish so coverage would end cleanly with no hole. I then checked whether chunk
144 had completed by looking for its `.out` file, saw only a `.tmp`, concluded it
had been killed, and relaunched it. That was wrong — chunk 144 was still running;
the `.tmp` was simply the in-progress file. The relaunch opened the *same*
`.tmp` with `>` and truncated it, so two processes were writing one file.

Caught it three minutes later by reading the actual process tree instead of
inferring from filenames, and killed the duplicate. Two things limited the
damage: neither process had written output yet (search647 only prints at the
end), and `progress647.py` accepts a chunk only if it carries the exact
terminal `# range [L,R) done` line — so a corrupted chunk shows up as a *hole*
that stops the reported bound, never as a silently wrong bound. The fail-safe
was doing its job. Verified chunk 144's file when it landed.

Lesson kept: check the process tree, not the filesystem, before concluding a job
died.
